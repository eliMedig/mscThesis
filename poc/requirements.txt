# TEAF PoC dependencies — pinned to the versions the PoC was built and tested on
# (Python 3.11+; developed on 3.12). Install: pip install -r poc/requirements.txt

# --- UI (Knowledge Interface) + config ---
streamlit==1.58.0
python-dotenv==1.2.2

# --- LLM access: provider-agnostic wrapper (Anthropic + OpenAI) ---
anthropic==0.109.2
openai==2.42.0

# --- RAG: vectors + local embeddings (no API key needed) ---
chromadb==1.5.9
sentence-transformers==5.6.0
# PDF text extraction for RAG uploads (pure-Python, no native build) — text PDFs only
pypdf==6.13.3

# --- Anomaly detection: hybrid pipeline ---
scikit-learn==1.9.0
pandas==3.0.3
numpy==2.4.6

# --- Tests ---
pytest==9.1.0
